<div class="d-flex">
<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
   <h6>posted by <%= link_to post.user.name, user_path(post.user) %></h6>
     <h6><%= pluralize(post.views, "view") %></h6>
  <div>
    </div>
  </div>
</div>
