<p style="color: green"><%= notice %></p>

  <div class="btn-group mt-5 mb-5">
    <%= link_to "Back to categories", categories_path  , class:"btn btn-primary m-2" %>
    <% if current_user&.admin? %>
    <%= link_to "Edit this category", edit_category_path(@category), class:"btn btn-warning m-2" %>
    <%= button_to "Destroy this category", @category, method: :delete, class:"btn btn-danger m-2" %>
    <% end %>
  </div>

<%= render @category %>
<div>
 
</div>
